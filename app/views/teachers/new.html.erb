<%= form_for @teacher, :url => "/teachers" do |f| %>
  <p>
    <%= f.label :name, t("form.teacher.name") %>
    <%= f.text_field :name %>
  </p>

  <p class="submit">
    <%= f.submit %>
  </p>
<% end %>